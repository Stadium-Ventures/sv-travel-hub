// Moved to api/_data/ncaaSchools.ts (shared with the serverless health
// monitor — Vercel functions can't import from src/). Re-exported here so
// app imports keep working unchanged.
export { D1_BASEBALL_SLUGS } from '../../api/_data/ncaaSchools'
