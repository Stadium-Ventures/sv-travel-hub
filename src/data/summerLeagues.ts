// Moved to api/_data/summerLeagues.ts (shared with the serverless health
// monitor, which can't import from src/). Re-exported so app imports are
// unchanged.
export * from '../../api/_data/summerLeagues'
